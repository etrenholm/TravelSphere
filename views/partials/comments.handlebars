<div class="itinerary-comments">
    <div class="itinerary-comment">
        <h4>Comments:</h4>

        {{#each this}}
        <div class="comments">
            <p class="blue-text">{{member.username}} says:</p>
            <p>{{comment_text}}</p>
        </div>
        {{/each}}
    </div>
</div>   